# 統一換行符號為 LF
# 理由：程式碼實體放在 Windows 檔案系統，但實際由 WSL 的 Linux 工具建置與執行。
#       Windows 編輯器若存成 CRLF，shell script 的 shebang 會失效。
* text=auto eol=lf

# 二進位檔不做換行轉換
*.png binary
*.jpg binary
*.pdf binary
*.vhdx binary
